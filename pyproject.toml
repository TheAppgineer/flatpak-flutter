[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.8"
pyyaml = "^6.0.2"
toml = "^0.10.2"
packaging = "^25.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
